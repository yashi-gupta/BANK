<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kiitapp.com.investmentintender">

    <application
        android:allowBackup="true"
        android:icon="@drawable/emi"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EMI" >

        </activity>
        <activity android:name=".EmiResult" />
        <activity android:name=".CompareLoan">

        </activity>
        <activity android:name=".PPFCalculator">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".List" />
        <activity android:name=".PpfInfo"></activity>
    </application>

</manifest>